name: Build Windows

on:
  push:
    branches: [master, main]
  pull_request:
    branches: [master, main]

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.22'

      - name: Build
        run: go build -ldflags="-H windowsgui" -o simplyauto.exe ./cmd/simplyauto

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: simplyauto-windows
          path: simplyauto.exe
          retention-days: 30
